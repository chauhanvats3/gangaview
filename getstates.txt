

/*
    res = [];
for(let i=1;i<temp.children.length;i++){
    res.push({value:temp.children[i].value,text:`${temp.children[i].innerHTML}`,index:i-1})
}


/*

res=[];
function createResult(j){

    var xmlhttp;
      xmlhttp=new XMLHttpRequest();


    xmlhttp.open("GET","/frro/FormC/getvisasubtype?ajaxstate="+visa.children[j].value+"&valname=applicant_visatype&flag="+false,false);
    xmlhttp.send();

if (xmlhttp.status === 200) {
    console.log("Success Response "+j)
        console.log("Success Response "+xmlhttp.responseText)

        document.getElementById('citydata').innerHTML=xmlhttp.responseText;
       child = document.getElementById("applicant_visatype");
    let childs = [];
for(let i=1;i<child.children.length;i++){
    childs.push({value:child.children[i].value,text:`${child.children[i].innerHTML}`,index:i-1})
}
res.push({value:visa.children[j].value,text:visa.children[j].innerHTML,index:j,subtypes:childs});

j++;
if(j<visa.children.length){
    createResult(j);
}else{
        console.log("finieshed");
    return;
}
}
}


